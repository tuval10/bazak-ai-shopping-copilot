# Architecture — moved

The architecture content (system overview, framework choice, components, request lifecycle, the bounded
supervisor loop, retrieval strategy, the HTTP/streaming API, the data model, evaluation, and the tech
stack) now lives in **[README.md](README.md)** — see the *Architecture & Framework Choice*, *Retrieval
Strategy*, *Conversation & State Management*, and *Evaluation Strategy* sections.

The *why* behind each choice remains in **[DECISIONS.md](DECISIONS.md)** (the `Dn` decision log).
